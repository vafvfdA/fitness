# 0003 数据库迁移测试计划

## 测试范围

本测试计划验证后端数据库迁移基础是否可用。

## 自动化测试

| 编号 | 命令或测试 | 预期结果 | 实际结果 | 状态 |
| --- | --- | --- | --- | --- |
| T-1 | `mvn test` | 所有后端测试通过。 | 通过；共 3 个测试，失败 0，错误 0，跳过 0。 | 通过 |
| T-2 | `DatabaseMigrationTest` | Flyway 可以在 H2 MySQL 模式下执行迁移。 | 通过；Flyway 成功执行 `V1__create_core_tables.sql`。 | 通过 |
| T-3 | 表存在性检查 | 13 张核心表均存在。 | 通过；测试逐一检查核心表存在。 | 通过 |
| T-4 | 健康检查回归 | `/api/v1/health` 测试仍通过。 | 通过；原健康检查测试未回退。 | 通过 |

## 人工验收测试

| 编号 | 场景 | 步骤 | 预期结果 | 实际结果 | 状态 |
| --- | --- | --- | --- | --- | --- |
| M-1 | 检查迁移文件 | 查看 `backend/src/main/resources/db/migration`。 | 存在 `V1__create_core_tables.sql`。 | 通过；迁移文件已创建。 | 通过 |
| M-2 | 检查表设计覆盖 | 对照架构文档核心表列表。 | 迁移文件覆盖全部核心表。 | 通过；覆盖用户、资料、身体指标、训练、饮食、计划、知识库核心表。 | 通过 |

## 回归检查

- `[x]` 原有后端健康检查测试仍通过。
- `[x]` 未实现超出本变更范围的 CRUD。
- `[x]` 未执行 git commit 或 push。

## 红绿记录

- 红灯 1：首次执行迁移测试失败在测试元数据查询方式，修正测试后重跑。
- 红灯 2：迁移测试失败在 `user_account` 表不存在，说明测试准确覆盖缺少迁移脚本的问题。
- 绿灯：添加 `V1__create_core_tables.sql` 后，`mvn test` 通过。

## 未解决测试风险

- H2 MySQL 模式只能验证迁移基本可执行，不能完全替代真实 MySQL 验证。
- 尚未连接真实 MySQL 实例执行迁移。
