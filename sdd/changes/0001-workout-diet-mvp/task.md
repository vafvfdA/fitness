# 0001 训练饮食 MVP 任务

## 状态说明

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞
- `[-]` 延期

## 规划

1. `[x]` 根据 PRD 和架构文档创建 MVP 规格。
2. `[ ]` 解决 `spec.md` 中的开放问题。
3. `[ ]` 确认后端项目结构和包名。
4. `[ ]` 确认小程序项目结构和 UI 库选择。

## 后端基础

1. `[ ]` 初始化 Spring Boot 后端。
2. `[ ]` 添加数据库迁移工具。
3. `[ ]` 添加统一响应和错误模型。
4. `[ ]` 添加参数校验和全局异常处理。
5. `[ ]` 添加开发模式认证或微信登录边界。

## 数据模型

1. `[ ]` 添加用户、个人资料、身体指标相关迁移。
2. `[ ]` 添加训练记录和训练模板相关迁移。
3. `[ ]` 添加饮食记录和食物模板相关迁移。
4. `[ ]` 添加训练计划相关迁移。

## 训练模块

1. `[ ]` 先写训练总组数计算的失败测试。
2. `[ ]` 实现训练领域计算。
3. `[ ]` 先写按日期创建和查询训练记录的失败 API 测试。
4. `[ ]` 实现训练创建、查询、更新、删除 API。
5. `[ ]` 先写应用训练模板的失败测试。
6. `[ ]` 实现训练模板应用流程。

## 饮食模块

1. `[ ]` 先写饮食总热量计算的失败测试。
2. `[ ]` 实现饮食领域计算。
3. `[ ]` 先写按日期创建和查询饮食记录的失败 API 测试。
4. `[ ]` 实现饮食创建、查询、更新、删除 API。
5. `[ ]` 先写食物模板热量计算的失败测试。
6. `[ ]` 实现食物模板流程。

## 日历模块

1. `[ ]` 先写月度摘要聚合的失败测试。
2. `[ ]` 实现日历摘要服务。
3. `[ ]` 实现日历月度摘要 API。

## 计划模块

1. `[ ]` 先写目标体重差值计算的失败测试。
2. `[ ]` 实现个人资料目标计算。
3. `[ ]` 先写训练休息周期计算的失败测试。
4. `[ ]` 实现训练计划计算。
5. `[ ]` 实现个人资料和计划 API。

## 小程序前端

1. `[ ]` 初始化微信小程序前端。
2. `[ ]` 构建今日页快捷卡片。
3. `[ ]` 构建训练页面 A。
4. `[ ]` 构建饮食页面 B。
5. `[ ]` 构建日历页面。
6. `[ ]` 构建计划页面。
7. `[ ]` 添加 API 客户端和错误处理。
8. `[ ]` 添加加载、空状态和校验状态。

## 验证

1. `[ ]` 运行后端单元测试。
2. `[ ]` 运行后端 API 或集成测试。
3. `[ ]` 运行前端构建或小程序校验。
4. `[ ]` 完成人工验收检查。
5. `[ ]` 更新 `test.md`。
6. `[ ]` 完成 `review.md`。

