# 0001 训练饮食 MVP 测试计划

## 测试范围

本计划验证 MVP 的核心记录闭环：

- 训练记录。
- 饮食记录。
- 模板。
- 日历摘要。
- 个人体重目标。
- 训练计划。
- 用户数据隔离。

## 自动化测试

| 编号 | 命令或测试 | 预期结果 | 实际结果 | 状态 |
| --- | --- | --- | --- | --- |
| T-1 | 训练总组数计算单元测试 | 总组数等于所有组记录数量之和。 | 未执行 | 待执行 |
| T-2 | 训练创建和查询 API 测试 | 同一用户可以按日期查询到保存的训练。 | 未执行 | 待执行 |
| T-3 | 训练参数校验 API 测试 | 负数次数或负数热量被拒绝。 | 未执行 | 待执行 |
| T-4 | 训练模板应用测试 | 模板能生成可编辑的训练记录。 | 未执行 | 待执行 |
| T-5 | 饮食总热量单元测试 | 总热量等于食物条目热量之和。 | 未执行 | 待执行 |
| T-6 | 饮食创建和查询 API 测试 | 同一用户可以按日期查询到保存的饮食。 | 未执行 | 待执行 |
| T-7 | 饮食参数校验 API 测试 | 负数热量或非法数量被拒绝。 | 未执行 | 待执行 |
| T-8 | 食物模板计算测试 | 修改数量后得到预期热量估算。 | 未执行 | 待执行 |
| T-9 | 日历月度摘要测试 | 有记录的日期返回部位和热量摘要。 | 未执行 | 待执行 |
| T-10 | 目标体重计算测试 | 差值等于当前体重减去目标体重。 | 未执行 | 待执行 |
| T-11 | 训练周期计算测试 | 训练、休息和部位轮换得到预期今日计划。 | 未执行 | 待执行 |
| T-12 | 用户隔离测试 | 用户 A 不能读取或修改用户 B 的数据。 | 未执行 | 待执行 |

## 人工验收测试

| 编号 | 场景 | 步骤 | 预期结果 | 实际结果 | 状态 |
| --- | --- | --- | --- | --- | --- |
| M-1 | 记录训练 | 打开训练页面 A，选择日期，添加 2 个动作并添加组记录，保存。 | 摘要展示动作数量、总组数和热量。 | 未执行 | 待执行 |
| M-2 | 从训练跳到饮食 | 在训练页面 A 点击饮食入口。 | 饮食页面 B 使用同一日期打开。 | 未执行 | 待执行 |
| M-3 | 记录饮食 | 在午餐和晚餐下添加食物并保存。 | 摄入总热量更新。 | 未执行 | 待执行 |
| M-4 | 查看日历摘要 | 有记录后打开日历页面。 | 日期格子展示部位标签和训练热量。 | 未执行 | 待执行 |
| M-5 | 设置计划目标 | 设置当前体重和目标体重。 | 正确展示距离目标差值。 | 未执行 | 待执行 |
| M-6 | 空状态 | 打开没有记录的日期。 | 页面展示清晰的添加记录入口。 | 未执行 | 待执行 |

## 回归检查

- `[ ]` API 错误响应格式保持一致。
- `[ ]` 日期处理使用用户选定的本地日期。
- `[ ]` 删除记录后日历摘要同步更新。
- `[ ]` 应用模板不会修改原始模板。
- `[ ]` 前端能友好处理网络失败。

## 未解决测试风险

- 小程序测试方式尚未确定。
- 真实微信登录行为依赖小程序配置。
- 热量估算是近似值，需要产品文案评审。

